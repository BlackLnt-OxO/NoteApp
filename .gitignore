node_modules
dist/
dist-electron/
dist-electron-v*/
*.exe
.env
__pycache__/
*.pyc
*.png
*.jpg

# PDF annotation data (dev: project dir / packaged: userData)
pdf-annotations/

# Electron runtime userData (dev: project root) — notes, caches, prefs
DataLocation/

# PyInstaller engine build outputs — commit engine/cap-engine.spec only
engine/build/
engine/dist/
